We use simple JSON files to store translations for our application. Each language has its own JSON file, and the keys in these files correspond to the text elements in the app that need to be translated.

We'll use i18next later, if we end up supporting a lot of languages, but for now, KISS keep it simple stupid
